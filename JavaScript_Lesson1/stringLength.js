function calcStrLen(strOne, strTwo, strThree){
    totalLength = strOne.length + strTwo.length + strThree.length;
console.log(totalLength);
console.log(Math.floor(totalLength / 3));
}

calcStrLen('chocolate', 'ice cream', 'cake');